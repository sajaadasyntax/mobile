# 🚀 دليل البدء السريع - تطبيق المراجع العام

## ⚡ البدء خلال 5 دقائق

### 1. تثبيت المكتبات

```bash
cd apps/mobile
npm install
```

### 2. تشغيل التطبيق

```bash
npm start
```

ستفتح لك واجهة Expo. اختر أحد الخيارات:
- **مسح QR Code** من تطبيق Expo Go على هاتفك (متوفر على Android و iOS)
- **اضغط `a`** لفتح على محاكي Android
- **اضغط `i`** لفتح على محاكي iOS
- **اضغط `w`** لفتح على المتصفح

### 3. تسجيل الدخول

استخدم حساب مراجع عام:
- **اسم المستخدم:** (حساب AUDITOR من قاعدة البيانات)
- **كلمة المرور:** كلمة المرور المحددة

---

## 📱 تجربة على الهاتف مباشرة

### Android / iOS

1. **حمّل تطبيق Expo Go:**
   - Android: [Google Play Store](https://play.google.com/store/apps/details?id=host.exp.exponent)
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)

2. **شغّل التطبيق:**
   ```bash
   npm start
   ```

3. **امسح QR Code:**
   - من تطبيق Expo Go
   - سيتم تحميل التطبيق مباشرة

---

## 🔧 تعديل رابط API

إذا كان Backend على localhost أو خادم مختلف:

1. **افتح `app.json`**

2. **عدّل `apiUrl`:**
   ```json
   {
     "expo": {
       "extra": {
         "apiUrl": "http://YOUR_IP:4000/api"
       }
     }
   }
   ```

   > **ملاحظة:** استخدم IP الخاص بجهازك بدلاً من `localhost` للتجربة على الهاتف

3. **أعد تشغيل التطبيق:**
   ```bash
   # اضغط Ctrl+C لإيقاف
   npm start
   ```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: لا يمكن الاتصال بالـ API

**الحل:**
```bash
# تأكد أن Backend يعمل
pm2 status

# تأكد أن CORS يسمح بالاتصال
# في apps/api/src/index.ts تأكد من FRONTEND_URL
```

### المشكلة: "هذا التطبيق مخصص للمراجعين فقط"

**الحل:**
- تأكد أن المستخدم له role = 'AUDITOR'
- تحقق من قاعدة البيانات:
  ```sql
  SELECT username, role FROM users WHERE role = 'AUDITOR';
  ```

### المشكلة: شاشة بيضاء بعد التشغيل

**الحل:**
```bash
# امسح الكاش وأعد التثبيت
rm -rf node_modules
npm install
npm start --clear
```

---

## 📦 بناء APK للتوزيع

### بناء APK بسيط (للتجربة)

```bash
# تثبيت EAS CLI
npm install -g eas-cli

# تسجيل الدخول لـ Expo
eas login

# بناء APK
eas build --platform android --profile preview
```

سيتم رفع الكود وبناء APK على خوادم Expo، ثم سيعطيك رابط للتحميل.

### بناء محلي (بدون Expo servers)

يتطلب Android Studio:

```bash
# تصدير المشروع
npx expo prebuild

# بناء APK
cd android
./gradlew assembleRelease
```

ملف APK سيكون في: `android/app/build/outputs/apk/release/app-release.apk`

---

## 🎨 تخصيص التطبيق

### تغيير اسم التطبيق

في `app.json`:
```json
{
  "expo": {
    "name": "اسم جديد",
    "slug": "ma3mora-auditor"
  }
}
```

### تغيير الألوان

في `App.tsx`:
```typescript
const theme = {
  colors: {
    primary: '#YOUR_COLOR',
    secondary: '#YOUR_COLOR',
  },
};
```

### إضافة أيقونة وشاشة بداية

1. ضع الصور في مجلد `assets/`
2. حدث `app.json`:
   ```json
   {
     "expo": {
       "icon": "./assets/icon.png",
       "splash": {
         "image": "./assets/splash.png"
       }
     }
   }
   ```

---

## 📊 استخدام التطبيق

### الميزات المتاحة:

✅ **لوحة التحكم** - ملخص شامل للنظام  
✅ **سجلات المراجعة** - تتبع جميع العمليات  
✅ **الميزانية** - قائمة الدخل والميزانية  
✅ **تقرير المبيعات** - جميع فواتير المبيعات  
✅ **تقرير المشتريات** - جميع أوامر الشراء  

### الميزات غير المتاحة (Read-Only):

❌ إنشاء فواتير  
❌ تعديل بيانات  
❌ حذف سجلات  
❌ تأكيد عمليات  

---

## 💡 نصائح

1. **استخدم Pull to Refresh** لتحديث البيانات في أي شاشة
2. **استخدم البحث** في التقارير للوصول السريع
3. **اسحب للخلف** للرجوع للشاشة السابقة
4. **تأكد من اتصال الإنترنت** للوصول للبيانات الحديثة

---

## 📞 المساعدة

إذا واجهت أي مشكلة:

1. تأكد من تشغيل Backend
2. تحقق من رابط API في `app.json`
3. راجع سجلات الأخطاء في Terminal
4. تأكد من صلاحيات المستخدم (AUDITOR)

---

**سعيد بتجربتك! 🎉**

