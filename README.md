# 📱 Ma3mora Auditor Mobile App

تطبيق الجوال للمراجع العام - نظام معمورة لإدارة المخزون

## 🎯 الوصف

تطبيق React Native مبني باستخدام Expo مخصص لـ **المراجع العام (AUDITOR)** فقط. يوفر التطبيق واجهة محمولة لمراقبة وتتبع جميع العمليات في النظام بشكل للقراءة فقط (Read-Only).

## ✨ المميزات

### 🔐 **المصادقة**
- تسجيل دخول آمن للمراجع العام فقط
- التحقق من الصلاحيات تلقائياً
- تخزين الجلسة محلياً

### 📊 **التقارير والمراقبة**

#### **لوحة التحكم**
- ملخص المبيعات والمحصلات
- ملخص المشتريات
- ملخص المنصرفات
- الرصيد الصافي

#### **سجلات المراجعة**
- عرض جميع سجلات المراجعة
- تتبع جميع العمليات في النظام
- معلومات المستخدم والتاريخ
- بحث وتصفية السجلات

#### **الميزانية وقائمة الدخل**
- قائمة الدخل الشاملة
- الميزانية العمومية
- الأرباح والخسائر
- هامش الربح

#### **تقرير المبيعات**
- قائمة جميع فواتير المبيعات
- حالة الدفع والتسليم
- المجموع والمدفوع والمتبقي
- بحث بالفاتورة أو العميل

#### **تقرير المشتريات**
- قائمة جميع أوامر الشراء
- حالة الاستلام
- تأكيد الدفع
- بحث بالأمر أو المورد

## 🚀 التثبيت والتشغيل

### المتطلبات
```bash
- Node.js 18+
- npm or yarn
- Expo CLI
- Expo Go App (للتجربة على الهاتف)
```

### التثبيت

```bash
# الانتقال لمجلد التطبيق
cd apps/mobile

# تثبيت المكتبات
npm install
# أو
yarn install
```

### التشغيل

```bash
# تشغيل التطبيق
npm start
# أو
yarn start

# تشغيل على Android
npm run android

# تشغيل على iOS
npm run ios

# تشغيل على الويب
npm run web
```

## ⚙️ الإعدادات

### تحديث رابط API

في ملف `app.json`:

```json
{
  "expo": {
    "extra": {
      "apiUrl": "https://api.ma3morainventory.cloud/api"
    }
  }
}
```

## 📱 الاستخدام

### 1. **تسجيل الدخول**
- افتح التطبيق
- أدخل اسم المستخدم وكلمة المرور للمراجع العام
- سيتم رفض تسجيل الدخول لأي مستخدم غير مراجع

### 2. **لوحة التحكم**
- عرض ملخص شامل للنظام
- الوصول السريع للتقارير
- تحديث البيانات بالسحب للأسفل (Pull to Refresh)

### 3. **التقارير**
- اختر التقرير المطلوب من القائمة
- استخدم البحث للتصفية
- اسحب للأسفل لتحديث البيانات

## 🏗️ هيكل المشروع

```
apps/mobile/
├── src/
│   ├── contexts/
│   │   └── AuthContext.tsx       # إدارة المصادقة
│   ├── screens/
│   │   ├── LoginScreen.tsx       # شاشة تسجيل الدخول
│   │   ├── DashboardScreen.tsx   # لوحة التحكم
│   │   ├── AuditLogsScreen.tsx   # سجلات المراجعة
│   │   ├── BalanceSheetScreen.tsx # الميزانية
│   │   ├── SalesReportScreen.tsx  # تقرير المبيعات
│   │   └── ProcurementReportScreen.tsx # تقرير المشتريات
│   ├── services/
│   │   └── api.ts               # اتصال API
│   └── utils/
│       └── formatters.ts        # دوال التنسيق
├── App.tsx                       # نقطة الدخول
├── app.json                      # إعدادات Expo
└── package.json                  # المكتبات

```

## 🔒 الأمان

- ✅ تسجيل دخول آمن
- ✅ التحقق من صلاحيات AUDITOR فقط
- ✅ تخزين آمن للجلسة
- ✅ جميع الواجهات للقراءة فقط (Read-Only)
- ✅ لا يمكن إنشاء أو تعديل أو حذف البيانات

## 📦 بناء التطبيق للإنتاج

### Android (APK)

```bash
# بناء APK
eas build --platform android --profile preview

# أو بناء AAB للنشر على Google Play
eas build --platform android --profile production
```

### iOS (IPA)

```bash
# بناء للتوزيع
eas build --platform ios --profile production
```

> **ملاحظة:** يتطلب حساب Expo وإعداد ملف `eas.json`

## 🎨 التخصيص

### الألوان
يمكن تغيير الألوان الرئيسية في `App.tsx`:

```typescript
const theme = {
  colors: {
    primary: '#2563eb',    // اللون الأساسي
    secondary: '#7c3aed',  // اللون الثانوي
  },
};
```

### الأيقونات والشعار
ضع الملفات في:
- `assets/icon.png` - أيقونة التطبيق
- `assets/splash.png` - شاشة البداية
- `assets/adaptive-icon.png` - أيقونة Android

## 🐛 استكشاف الأخطاء

### مشكلة الاتصال بالـ API
```bash
# تأكد من رابط API في app.json
# تأكد من تشغيل Backend على الخادم
# تحقق من إعدادات CORS
```

### مشكلة تسجيل الدخول
```bash
# تأكد أن المستخدم له صلاحية AUDITOR
# تحقق من اسم المستخدم وكلمة المرور
```

## 📝 API المستخدمة

التطبيق يتصل بـ API التالية فقط:

- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/logout` - تسجيل الخروج
- `GET /api/accounting/audit-logs` - سجلات المراجعة
- `GET /api/accounting/balance/summary` - ملخص الأرصدة
- `GET /api/sales/invoices` - فواتير المبيعات
- `GET /api/procurement/orders` - أوامر الشراء
- `GET /api/accounting/expenses` - المنصرفات

## 📄 الترخيص

هذا المشروع خاص بنظام معمورة لإدارة المخزون.

## 👨‍💻 المطور

تم تطويره بواسطة فريق معمورة

---

**الإصدار:** 1.0.0  
**آخر تحديث:** 2024

